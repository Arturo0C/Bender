import java.util.HashMap;
import java.util.Map;

public class main {
    public static void main(String[] args) {
        Map<Integer, Teleporter> tpList = new HashMap<>();
        Teleporter tp;

       double resultado = Math.sqrt(Math.pow(1-3,2) + Math.pow(1-3,2));
        System.out.println(resultado);
    }



}